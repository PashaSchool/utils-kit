{
  "private": true,
  "name": "utils-kit",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/playground dev",
    "check:write": "npx @biomejs/biome check --write ./packages/**/src",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "husky": "^9.1.7"
  }
}
